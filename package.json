{
  "name": "tla-tools-action",
  "version": "1.0.0",
  "private": true,
  "description": "GitHub Action for installing immutable TLA+ tools releases",
  "license": "MIT",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "npm run build && npm run lint && npm test && npm run check:dist",
    "check:dist": "git diff --exit-code -- dist",
    "lint": "node --check dist/action.js && node --check dist/contracts.js && node --check dist/download.js && node --check dist/github.js && node --check dist/index.js && node --check dist/tlc.js && node --check tests/action.test.js && node --check tests/setup-contract.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "24.13.3",
    "typescript": "7.0.2"
  }
}
